spring.application.name=Userservice
  # Corrected to align with package name convention

# Database configuration - Credentials should be externalized
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.odffpjvuptssxaaggnsn&password=Postgres
#spring.jpa.hibernate.ddl-auto=none
spring.datasource.username=${DB_USER:postgres.odffpjvuptssxaaggnsn}
spring.datasource.password=${DB_PASSWORD:Postgres} 
  # Externalize this!

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update 
  # Use 'validate' or 'none' in production after initial schema setup
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
 # Set to true for debugging, false for production

logging.level.org.springframework.web.cors=INFO
logging.level.org.springframework.security=INFO
  # Set to DEBUG for JWT/Keycloak troubleshooting
logging.level.com.example.Userservice=INFO
 # Corrected package name

# Keycloak Issuer URI for JWT validation
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:https://gotogetheruom.duckdns.org:8446/realms/kong}

# gRPC Server Configuration for user-service
grpc.server.port=${GRPC_SERVER_PORT:9092}
  # Define a port for user-service gRPC, e.g. 9092

# Server port for Spring MVC (REST controllers, if any)
server.port=${APP_PORT:8082}
   # Define a port for user-service REST, e.g. 8082
